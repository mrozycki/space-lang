func sum numbers from 1 to `n` {
    if n < 1 { return 0; }
    return n + sum numbers from 1 to (n - 1);
}

func count down from `n` to 1 {
    if n < 1 { return; }
    println(n);
    count down from (n - 1);
}